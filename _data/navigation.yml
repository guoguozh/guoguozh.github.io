# main links links
main:
  # Publications is disabled for now — the list lives on the homepage instead.
  # To re-enable: uncomment the two lines below and set `published: true` in
  # _pages/publications.md.
  # - title: "Publications"
  #   url: /publications/

  - title: "Research"
    url: /research/


  - title: "Work"
    url: /work/    
    
  - title: "Teaching"
    url: /teaching/    
    
  # Portfolio is disabled for now (content kept in _portfolio/).
  # To re-enable: uncomment the two lines below, set `published: true` in
  # _pages/portfolio.html, and set the portfolio collection back to
  # `output: true` in _config.yml.
  # - title: "Portfolio"
  #   url: /portfolio/

  - title: "Clinical"
    url: /clinical/
    
  - title: "Horizontal"
    url: /projects/
